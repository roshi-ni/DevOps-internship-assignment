name: DevOps Internship CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Build Docker Image
        run: |
          docker build -t hello-java-app .

      - name: Run Tests
        run: |
          echo "Tests passed"

      - name: Simulate Docker Push
        run: |
          echo "Docker image would be pushed to DockerHub here"
